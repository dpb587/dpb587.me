---
description: Global instructions for the project.
applyTo: '**'
---

To rebuild after style and script changes: run `./build.sh` (from the `hugo/themes/current` directory).

To test rendering after template changes: run `hugo --renderToMemory` (from the `hugo` directory).
